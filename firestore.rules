rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read and write for demo/testing
    // IMPORTANT: Change these rules for production!
    match /{document=**} {
      allow read, write: if true;
    }
  }
}


